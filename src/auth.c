#include "auth.h"

int auth_parse(const unsigned char *data, size_t len,
               uint32_t *token, uint16_t *port, uint32_t *lifetime)
{
    (void)data;
    (void)len;
    (void)token;
    (void)port;
    (void)lifetime;
    return -1;
}
