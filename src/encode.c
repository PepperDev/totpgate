#include "encode.h"

int secret_decode(const char *input, unsigned char *out, size_t *out_len,
                  enum secret_encoding *encoding)
{
    (void)input;
    (void)out;
    (void)out_len;
    (void)encoding;
    return -1;
}
